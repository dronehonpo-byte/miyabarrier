# コントリビューションガイド

Miyabarrier は「営業文面のパターンをみんなで育てる」ことを前提にしたライブラリです。コードを書かない貢献がいちばん効きます。

## いちばん歓迎する貢献: NG ワードの追加

実際に届いた迷惑送信で、いまの Miyabarrier が見逃した表現を [`patterns/ng-words.json`](./patterns/ng-words.json) に足してください。

1. 既存カテゴリに合う語なら、そのカテゴリの `terms` に 1 語ずつ追加します。`score` と `cap` は変えません。
2. 新しい分野（業種特有の売り込みなど）なら、新カテゴリを作ります。
   - `id` は kebab-case
   - `score` は 1〜4（絶対値）が目安。決定的な語ほど大きく
   - `cap` は `score` と同じ符号で、`score` の 2〜4 倍程度
3. `terms` は**小文字・半角**で書きます（照合前に NFKC 正規化と小文字化をするため、`ＳＥＯ` と書く必要はありません）。
4. `npm test` を実行してください。重複登録・符号の誤り・正規表現の誤りは自動で検出されます。

```jsonc
{
  "id": "insurance-sales",
  "label": "保険の売り込み",
  "score": 3,
  "cap": 9,
  "terms": ["保険の見直し", "無料保険相談"],
}
```

### 誤検知の報告も同じくらい重要です

「普通の問い合わせがブロックされた」という報告は、NG ワードの追加よりも優先度が高いです。Issue に次を書いてください。

- ブロックされた本文（個人情報は伏せてください）
- 画面に出た「判定の理由」
- 可能なら `data-debug="true"` で表示される内訳

対処は次のどれかになります。

- 減点カテゴリ `legit-inquiry` に語を足す
- `allowlist.terms` にフレーズを足す（そのフレーズ内でしか現れない NG ワードを打ち消す）
- 該当カテゴリの `score` / `cap` を下げる

## 配点・しきい値の変更

[`patterns/weights.json`](./patterns/weights.json) を変えるときは、`packages/core/tests/scoring.test.ts` が校正記録になっています。実シナリオのスコアが動くので、**テストを通したうえで、意図した変化であることを PR の説明に書いてください**。

調整の順番は しきい値 → `layers[].weight` → 個別の `points` が目安です。`layers[].weight` はグループ内で合計 1.0 になるようにしてください（ビルド時に検証されます）。

## 開発環境

```bash
npm install
npm test          # ユニットテスト
npm run verify    # format:check + lint + typecheck + test + build（CI と同じ）
npm run demo      # http://localhost:4173 でデモを起動
```

`patterns/*.json` を編集したら `npm run gen` を実行してください（`packages/core/src/patterns.data.ts` に埋め込まれます）。忘れてもテストが検出します。生成物は直接編集しないでください。

## コードを書く場合の約束

- **`packages/core` は DOM とネットワークに触らない。** 判定はすべて純関数で、入力はスナップショットだけです。この境界がテスタビリティを支えています。
- **レイヤーは点数を持たない。** 出すのはシグナルだけで、配点は `weights.json` に置きます。
- **新しいシグナルは 3 箇所を同時に更新する。** 実装・`signals.ts` のラベル・`weights.json` の配点。対応漏れはテストが落とします。
- **誤検知側のテストを必ず書く。** 「対象で立つ」テストだけでは不十分です。「人間では立たない」テストを必ず添えてください。
- 判定できないときは `applicable: false` を返します。無理に 0 点を返すと、それが「人間らしさの証拠」として平均に混ざります。

新しいレイヤーの追加手順は [docs/how-it-works.md](./docs/how-it-works.md#拡張する) にまとめてあります。

## Pull Request

- 1 PR = 1 目的（NG ワードの追加と配点の変更は分けてください）
- `npm run verify` が通ること
- `dist/` の再生成は不要です（リリース時にまとめて更新します）

## 行動規範

敬意をもって議論してください。営業行為そのものを否定するプロジェクトではなく、「受け取る側が受け取り方を選べるようにする」ためのツールです。
