# 変更履歴

このプロジェクトは [Semantic Versioning](https://semver.org/lang/ja/) に従います。

## [0.5.0] — 2026-08-20

### 削除

- **`packages/dashboard` を削除しました。** 検知ログの可視化画面、しきい値シミュレーター、
  「お返しの営業（下書き）」の一覧、および `scripts/build-dashboard.mjs` が対象です。
  GitHub Pages の `/packages/dashboard/` も無くなります。

### 変更

- 判定ログと「お返しの営業」の下書きは **API から扱う**形になりました。localStorage への
  保存自体は従来どおり動きます。

  ```js
  Miyabarrier.getLog(); // 判定ログ
  Miyabarrier.getCounterQueue(); // お返しの営業の下書き
  Miyabarrier.counterMailtoUrl(draft); // mailto: の URL（今回追加）
  ```

- `Miyabarrier.counterMailtoUrl()` を公開 API に追加。ダッシュボードの「メールソフトで開く」
  ボタンが無くなったため、同じ導線をコードから作れるようにした。
- README の「ダッシュボード」の節を「判定ログの見方」に差し替え、しきい値を決める手順を
  API ベースに書き直した。
- テストは 164 件 → 143 件（ダッシュボードの集計テスト 21 件が対象外になったため）。
  widget / core のテストは変更していない。

## [0.4.1] — 2026-08-20

### 修正

- **ブロックパネルが入力の書き換え後も残り続ける不具合を修正。** 判定パネル（スコア内訳・判定理由・
  お返しの営業の文面）が、入力の変更で消えずに古い結果を表示し続けていた。
  チェック行のリセットとパネルのクリアが別々の場所にあり、後者が漏れていた。
  両方を `clearVerdictUi()` にまとめ、必ず同時に消えるようにした。
- あわせて、リセットの登録位置を `mountCheckbox()` からコンストラクタへ移した。
  チェックボックス UI を無効にしたサイト（`data-checkbox="false"`）では
  リセットが一度も登録されず、パネルが永久に残る状態だった。
- 回帰テストを 4 件追加（合計 164 件）。修正前のコードでは 3 件が落ちることを確認済み。

## [0.4.0] — 2026-08-20

### 追加

- **お返しの営業（カウンターピッチ）** — 営業と判定した送信に対して、あらかじめ用意した自社の営業文を
  組み立てる機能（`counter` オプション、既定は無効）。ブロック画面にその場で表示し、
  同時に下書きを送信箱（localStorage）へ積む。
  - **自動送信はしない。** 特定電子メール法のオプトイン原則、なりすまし宛先への加害（バックスキャッタ）、
    自社ドメインの配信評価の 3 点から、宛先と内容を人間が確認して自分のメールソフトから送る形にした。
    自社サーバーで自動化したい場合のために `onCounter` フックを用意している
  - 同じ宛先は重ねて積まない／アドレスの形式を検査する／件数上限を設ける、の 3 段で誤爆を防いでいる
  - ダッシュボードに「お返しの営業（下書き）」を追加。mailto: で自分のメールソフトを開く導線つき
- **チェック行の状態表示** — 送信できたときに「確認しました」、止めたときに「送信を止めました」を
  その行に出す。サイト側の完了画面が出るまで何も起きないように見えるのを解消した。
- **`examples/embedded.html`** — よくある工務店サイト風の独自 CSS のページに 1 行だけ埋め込んだ状態。
  ライブラリのデザインを読み込まないページで、浮かないか・壊れないかを確認するためのもの。
- `collectFormValues()` がメールアドレスを返すようになった（お返しの営業の宛先に使う）。

### 修正

- インラインのバッジが送信ボタンと同じ行に並んでしまうのを直した（独立した行にした）。

## [0.3.0] — 2026-08-20

見た目の全面刷新。判定ロジック（core / aggregate）は変更していない。

### 追加

- **`packages/design`** — デザイントークン（色・書体・角丸・意味色）、共通 UI の CSS、Miyabee のロゴマーク。
  widget は TypeScript から import してバンドルに埋め込み、dashboard / demo / トップページは
  `scripts/build-design.mjs` が書き出す `assets/` を読む。定義元はこの 1 か所だけ。
  - ロゴは「120 度ずつ配置した 3 つの輪 × 同心楕円」をパラメトリックな SVG として再構成した。
    ラスタ画像を持たないので任意サイズで鮮明、色はトークンから差し替わる。24px 以下向けの簡略プリセットつき
  - Web フォントは使わない（外部通信ゼロという製品性質を守るため）
- **`assets/logo.js`** — 静的ページ用の軽量なロゴ注入スクリプト。ロゴのために widget 全体を読み込まずに済む。
- ブロックパネルに **automation / sales のグループ別スコアバー**を追加。「なぜ止まったか」が一目で分かる。
- `Miyabarrier.markSvg()` を公開 API に追加。

### 変更

- **widget のトークンを `:root` から `.mb-root` に移した。** 埋め込み先サイトのカスタムプロパティを
  汚さないため。埋め込み型のライブラリが `:root` に変数を撒くのは行儀が悪い。
- ブロックパネル・チェックボックス・バッジを刷新。意味色は「左端 3px の帯・細いバー・小さなピル」だけに使い、
  面で塗るのをやめた（黄×黒の警告テープ感を避けるため）。
- `data-debug="true"` の内訳を、レイヤー別スコアバー + シグナル名のチップに変更。
  レイヤー名は短縮名（`L2.5 環境` など）にして、狭い幅でも省略されないようにした。
- チェックボックスのチェックマークを data URI の SVG で描画（外部リソースを増やさない）。
- ダッシュボードを KPI カード + 検知理由の内訳 + しきい値シミュレーター（差分表示つき）に再構成。
  スコア分布にはしきい値の位置を破線で重ねた。
- デモを「保護なし」「保護あり」の左右比較に変更。同じ文面で同時に試せる。
- トップページ（GitHub Pages）を共通デザインに統一。
- UI を内蔵したぶん widget は gzip 21KB → 25KB になった。ランタイム依存は 0 のまま。

## [0.2.0] — 2026-08-20

### 追加

- **`packages/dashboard`** — 判定ログを可視化する静的ページ（サーバー不要・外部通信なし、gzip 4KB）。
  判定の内訳・日別件数・スコア分布・多かった判定理由・ページ別集計を表示する。
  - **しきい値シミュレーター** — 過去のログを別のしきい値で再判定し、変化する件数を表示する。
    `report` モードで溜めたログから、自分のサイトに合うしきい値を決めるための機能。
  - localStorage はオリジンごとに分かれるため、同一オリジンでの自動読み込みに加えて
    JSON の貼り付けによる取り込みにも対応した。
- widget に `logLimit` オプション（`data-log-limit`、既定 200）。観察期間に合わせてログ保持件数を変えられる。
- 判定ログに `hard`（即時ブロックだったか）を記録。しきい値の後追い検証に必要なため。
- テストを 143 件に拡充（dashboard の集計 22 件を追加）。

### 変更

- ログの localStorage 入出力を `packages/widget/src/log.ts` に分離した。
  dashboard がキーを共有するために import しても、widget の自動初期化が走らないようにするため
  （import 一発で保護処理まで動くと、ダッシュボード自身のフォームを保護してしまう）。
- 判定ログの既定保持件数を 50 → 200 に増やした。1〜2 週間の観察には 50 件では足りないため。

### 修正

- ダッシュボードのヒストグラムで、しきい値がビンの内側に落ちる場合の区分色が実際の判定とずれていた
  （中央値で判定するようにした）。
- しきい値シミュレーターで「通過が増える」変化が警告色で表示されていた。判定が厳しくなる向きを
  警告色、緩くなる向きを安全色に統一した。
- 確認しきい値をブロックしきい値より上に動かしたとき、スライダーのつまみと表示値がずれていた。

## [0.1.0] — 2026-08-20

最初のリリース。

### 追加

- **`@miyabarrier/core`** — 判定レイヤーとスコアリング。DOM・ネットワークに非依存の純関数群。
  - Layer 1 ハニーポット（隠しフィールド・おとりチェックボックス・署名付きトークン）
  - Layer 2 行動解析（経過時間・ポインタ／タッチ・打鍵速度・貼り付け）
  - Layer 2.5 自動化ブラウザの痕跡（`navigator.webdriver`・headless UA・環境の矛盾）
  - Layer 2.6「不自然な自然さ」検知（速度の変動係数・軌跡の直線度・手ぶれ・打鍵間隔の量子化）
  - Layer 3 チェックボックス認証（`isTrusted`・チェック時刻・ポインタ操作の有無）
  - Layer 4 営業文面判定（NG ワードのスコアリング・URL 数・自己紹介・署名ブロック）
  - Layer 6 AI 生成文っぽさ判定（文長のばらつき・丁寧表現の密度・リズム・崩れの無さ）
  - グループ別加重平均 + noisy-or による統合スコアリング
- **`@miyabarrier/widget`** — スクリプトタグ 1 行で動くブラウザ側。フォームの自動検出、ハニーポット注入、
  操作の計測、チェックボックス／バッジ／判定パネルの UI、`submit` のフック。
  `block` / `warn` / `report` の 3 モード。
- **`patterns/ng-words.json`** — 13 カテゴリ・243 語の営業文面パターン（日本語・英語）。
  カテゴリごとの上限、正当な問い合わせ語による減点、allowlist による誤検知抑制。
- **`patterns/weights.json`** — 配点・しきい値・レイヤー固有のチューニング値。JSON Schema 付き。
- **`examples/demo.html`** — リアルタイム文面スコアと bot シミュレーション（ハニーポット／即時送信／模倣操作）付きのデモ。
- テスト 122 件（core 92 / widget 30）、CI と GitHub Pages のワークフロー。

### 設計上の決定（実機検証を経て変更した点）

- **グループ別 noisy-or での合成** — 当初は 7 レイヤーの単純な加重平均だったが、人が手打ちした営業メールで
  automation 側のシグナルがすべて 0 になり、文面の証拠が薄まってブロックできなかった。「機械か」と
  「営業か」を独立した疑いとして分離した。
- **ハニーポットの `evidenceOnly`** — 無反応のハニーポットを母数に入れると、明白な bot 送信が `review` に
  留まってしまう。正直な利用者も対策済みの bot も同じく無反応になるため、沈黙は証拠として扱わない。
- **IME 経由の日本語入力** — Chrome では IME が処理したキーの `keydown` は `key === 'Process'` になる。
  これを打鍵として数えていなかったため、日本語で入力した人が全員 `noKeystrokes` と判定されていた。
  あわせて `input` イベントも計測し、音声入力・オートフィル・`insertText` にも対応した。
- **`Electron/` を自動化マーカーから除外** — アプリ内ブラウザ（Slack や各種デスクトップアプリの WebView）は
  実ユーザーが使うため、自動化の証拠にならない。
- **クリックをポインタ操作として計上** — マウスを動かさずタブ移動とクリックで入力する人を bot 扱いしないため、
  `pointerdown` も軌跡サンプルに含める。
- **Layer 2.6 は単独ではブロックに届かない配点** — 視線入力やスイッチ入力の利用者は操作が機械的に均一に
  なりうるため、統計的な均一さだけで送信を止めない。
